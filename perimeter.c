

777777//Write a C program to input the length and width of a rectangle and find its perimeter.
void main()
{
	int length=400,width=600,rect,perimeter;
	rect=length*width;
	printf("area of rectangle is %d",rect);
	perimeter=2*(length*width);
	printf("\n area of perimeter rectangle %d",perimeter);
}